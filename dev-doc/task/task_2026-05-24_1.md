---
title: TASK - 重构 agent 输入系统（项目上下文 + 模式感知）
nums: 7
---

- [x] T1：实现 scripts/lib/context.sh 项目上下文扫描
  - level: P0
  - details：创建 `scripts/lib/context.sh`，实现 `project_context()` 函数。扫描项目根目录，输出结构化摘要（目录结构、技术栈推断、已有测试结构、运行方式、现有模块列表）。使用 `tree -L 2` 生成目录结构，若 tree 不可用则 fallback 到 `find` 格式化输出。硬限 200 行上限，超出时用 `head` 截断。空项目不报错，输出"空项目"提示。脚本接受可选参数 `[项目根目录]`，默认为当前目录。
  - depends on：无
  - Done when：`bash scripts/lib/context.sh /home/xinyue/mythology/dev-flow | wc -l` 输出不超过 200；`bash scripts/lib/context.sh /home/xinyue/mythology/dev-flow` 输出包含 "技术栈" 和 "目录结构"；`bash scripts/lib/context.sh /tmp/empty_dir_test` 不报错且退出码为 0（需先 `mkdir -p /tmp/empty_dir_test`）；在没有 tree 命令的环境中 `PATH="" bash scripts/lib/context.sh .` 不报错（fallback 验证）

- [x] T2：改造 commands/spec.md 支持模式感知输入
  - level: P0
  - details：修改 `commands/spec.md`，将前置检查从硬性阻断改为模式感知逻辑。full 模式：PRD.md 必须存在（行为不变）。quick/mvp 模式：PRD.md 不存在时使用 BRAINSTORM.md（如有）或用户描述作为替代输入源。所有模式均在 agent 输入中追加项目上下文（调用 `scripts/lib/context.sh`）。更新 agent 调度模板，将项目上下文作为输入文档的一部分传入子代理。
  - depends on：T1
  - Done when：`grep -c "context" commands/spec.md` 大于 0；`grep -c "mode\|模式" commands/spec.md` 大于 0；文件中包含 full 模式要求 PRD.md 存在的逻辑；文件中包含 quick/mvp 模式的降级逻辑（grep 到 "BRAINSTORM\|用户描述" 的替代路径）

- [x] T3：改造 commands/task.md 支持模式感知输入
  - level: P0
  - details：修改 `commands/task.md`，在现有 fast 模式逻辑基础上，为所有模式追加项目上下文输入。full/quick 模式：SPEC.md 必须存在（行为不变）+ 项目上下文。fast 模式：用户描述 + 项目上下文（现有逻辑已支持不要求 SPEC.md，补充上下文即可）。更新 agent 调度模板。
  - depends on：T1
  - Done when：`grep -c "context" commands/task.md` 大于 0；agent 调度模板中包含项目上下文的传入说明；fast 模式部分包含"项目上下文"字样

- [x] T4：改造 commands/test.md、commands/devtest.md、commands/fix.md 追加项目上下文
  - level: P1
  - details：修改三个 command 文件，在 agent 调度模板中追加项目上下文输入。test.md 额外修正：输入模板中"所有未完成 task 文件"改为"所有 task 文件（含 done_task_*）"；测试代码路径从 `test_<功能>.py` 改为 `test_<功能>.<ext>`（不硬编码 .py）。devtest.md：在验证目标后追加项目上下文。fix.md：在 issue 内容和相关规范后追加项目上下文。
  - depends on：T1
  - Done when：`grep -c "context\|上下文" commands/test.md` 大于 0；`grep -c "context\|上下文" commands/devtest.md` 大于 0；`grep -c "context\|上下文" commands/fix.md` 大于 0；`grep "test_.*\.py" commands/test.md` 无匹配（已移除硬编码 .py）；`grep "done_task" commands/test.md` 有匹配

- [x] T5：改造 agents/test-agent.md 移除硬编码语言扩展名
  - level: P1
  - details：修改 `agents/test-agent.md`，将 `test_<功能>.py` 改为 `test_<功能>.<ext>`（或移除扩展名硬编码的表述）。保持测试代码规范部分的其他内容不变。
  - depends on：无
  - Done when：`grep "test_.*\.py" agents/test-agent.md` 无匹配（不含硬编码 .py 的路径模式）；文件中仍保留 `tests/` 目录的规范说明

- [x] T6：删除冗余 hook 文件 + 修正 marketplace.json
  - level: P1
  - details：删除已合并到 post-write.sh 的 4 个独立 hook 文件：`scripts/hooks/check-doc-sync.sh`、`scripts/hooks/check-phase-completion.sh`、`scripts/hooks/check-task-completion.sh`、`scripts/hooks/update-status.sh`。确认 hooks.json 中不引用这些文件。修正 `.claude-plugin/marketplace.json` 中 description 里的 "TEST → DONE" 改为 "TEST → ITERATE"（如存在该文本）。
  - depends on：无
  - Done when：`ls scripts/hooks/check-doc-sync.sh 2>&1` 输出 "No such file"；`ls scripts/hooks/check-phase-completion.sh 2>&1` 输出 "No such file"；`ls scripts/hooks/check-task-completion.sh 2>&1` 输出 "No such file"；`ls scripts/hooks/update-status.sh 2>&1` 输出 "No such file"；`grep -r "check-doc-sync\|check-phase-completion\|check-task-completion\|update-status" hooks.json hooks/hooks.json 2>/dev/null` 无匹配（确认无残留引用）；`grep "DONE" .claude-plugin/marketplace.json` 无匹配（已修正为 ITERATE，或原本不含该文本）

- [x] T7：端到端集成验证（context.sh + command 联动）
  - level: P2
  - details：在 tests/ 目录编写集成测试脚本 `tests/test_context_integration.sh`，验证：1) context.sh 在本项目中能正确输出且不超过 200 行；2) context.sh 在空目录中输出合理内容且退出码为 0；3) 模拟无 tree 命令环境验证 fallback；4) 验证各 command 文件中确实引用了项目上下文。这是对 T1-T4 的回归验证。
  - depends on：T1, T2, T3, T4
  - Done when：`bash tests/test_context_integration.sh` 全部测试 PASS 且退出码为 0
