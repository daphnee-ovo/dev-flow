---
title: TASK - Audit Mode 自动审计模式
nums: 6
---

- [x] TASK-T001: common.sh 新增 audit 辅助函数
  - priority: P0
  - refs: SPEC-AC-001, SPEC-AC-002
  - files:
      create: []
      modify: ["scripts/lib/common.sh"]
      test: ["tests/test_audit_mode.sh"]
  - depends_on: []
  - parallel: true
  - complexity: S
  - done_when:
      - "`source scripts/lib/common.sh && is_audit_mode 'audit/quick'` 退出码为 0"
      - "`source scripts/lib/common.sh && is_audit_mode 'quick'` 退出码为 1"
      - "`grep -c 'is_audit_mode\|enter_audit_mode' scripts/lib/common.sh` 输出 >= 2"

- [x] TASK-T002: mode.sh 拒绝手动设置 audit + 文档同步
  - priority: P0
  - refs: SPEC-AC-004
  - files:
      create: []
      modify: ["scripts/commands/mode.sh", "commands/mode.md", "CLAUDE.md", "README.md", "README.zh-CN.md"]
      test: ["tests/test_audit_mode.sh"]
  - depends_on: []
  - parallel: true
  - complexity: S
  - done_when:
      - "`bash scripts/commands/mode.sh audit dev-doc 2>&1` 输出包含 '不支持手动设置' 且退出码为 1"
      - "`bash scripts/commands/mode.sh audit/quick dev-doc 2>&1` 输出包含 '不支持手动设置' 且退出码为 1"
      - "`bash scripts/commands/mode.sh quick dev-doc 2>&1` 正常执行（不被拒绝）"
      - "commands/mode.md 中包含 audit 模式说明（自动触发、不可手动设置）"
      - "CLAUDE.md 模式表中提及 audit"
      - "README.md 和 README.zh-CN.md 开发模式章节提及 audit"

- [x] TASK-T003: post-write.sh 检测 issue 创建并自动进入 audit 模式
  - priority: P0
  - refs: SPEC-AC-001
  - files:
      create: []
      modify: ["scripts/hooks/post-write.sh"]
      test: ["tests/test_audit_mode.sh"]
  - depends_on: ["TASK-T001"]
  - parallel: false
  - complexity: M
  - done_when:
      - "准备 STATUS.yaml（mode: quick, phase: SPEC）+ 模拟写入 dev-doc/issue/issue_test.md 后，STATUS.yaml 中 mode 变为 `audit/quick`、phase 变为 `DEV`"
      - "已处于 audit/quick 时再写入 issue 文件，mode 不变（不重复触发）"
      - "phase 为 DEV 时写入 issue 文件，mode 不变（DEV 阶段不触发）"

- [x] TASK-T004: iterate.sh audit 模式恢复逻辑
  - priority: P0
  - refs: SPEC-AC-003
  - files:
      create: []
      modify: ["scripts/commands/iterate.sh", "commands/iterate.md"]
      test: ["tests/test_audit_mode.sh"]
  - depends_on: ["TASK-T001"]
  - parallel: false
  - complexity: M
  - done_when:
      - "STATUS.yaml mode 为 `audit/quick` 时执行 iterate，结束后 mode 恢复为 `quick`、phase 为 `SPEC`"
      - "STATUS.yaml mode 为 `audit/fast` 时执行 iterate，结束后 mode 恢复为 `fast`、phase 为 `TASK`"
      - "audit 模式下 iterate 跳过 task 完成度检查（无 task 文件不报错）"
      - "非 audit 模式 iterate 行为不变（原有测试继续通过）"
      - "commands/iterate.md 中包含 audit 模式恢复行为的说明"

- [x] TASK-T005: inject-context.sh audit 模式状态展示
  - priority: P1
  - refs: SPEC-AC-002
  - files:
      create: []
      modify: ["scripts/hooks/inject-context.sh"]
      test: ["tests/test_audit_mode.sh"]
  - depends_on: ["TASK-T001"]
  - parallel: true
  - complexity: S
  - done_when:
      - "STATUS.yaml 为 `mode: audit/quick` 时，inject-context 输出包含 `[dev-flow audit/quick]`"
      - "audit + DEV 阶段输出包含 `issue → /fix → /iterate 恢复原模式` 提示文案"

- [x] TASK-T006: commands/fix.md audit 模式流程提示
  - priority: P2
  - refs: SPEC-AC-003
  - files:
      create: []
      modify: ["commands/fix.md"]
      test: []
  - depends_on: []
  - parallel: true
  - complexity: S
  - done_when:
      - "`grep -c 'audit' commands/fix.md` 输出 >= 1"
      - "fix.md 中包含 audit 模式下 iterate 后自动恢复原模式的说明"
