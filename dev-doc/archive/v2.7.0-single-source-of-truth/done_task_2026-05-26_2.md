---
title: TASK - 单一真相源重构
nums: 8
---

- [x] TASK-T001: 升级 references/dev-doc/TASK-FILE.md 为新格式
  - priority: P0
  - refs: SPEC-AC-001
  - files:
      create: []
      modify: ["references/dev-doc/TASK-FILE.md"]
      test: ["tests/test_validate.sh"]
  - depends_on: []
  - parallel: true
  - complexity: S
  - done_when:
      - "references/dev-doc/TASK-FILE.md 模板使用 `TASK-T001:` 编号、`priority:`、`refs:`、`files:`、`depends_on:`、`parallel:`、`complexity:`、`done_when:` 字段，与 agents/task-agent.md 中定义一致"
      - "`grep -c 'TASK-T' references/dev-doc/TASK-FILE.md` 输出 >= 1"
      - "不再包含 `T1：`、`level:`、`details：`、`Done when：`、`depends on：` 旧字段"

- [x] TASK-T002: 新增 references/dev-doc/SPEC-FILE.md 和 STATUS.md
  - priority: P0
  - refs: SPEC-AC-001
  - files:
      create: ["references/dev-doc/SPEC-FILE.md", "references/dev-doc/STATUS.md"]
      modify: []
      test: ["tests/test_skills_docs.sh"]
  - depends_on: []
  - parallel: true
  - complexity: S
  - done_when:
      - "`test -f references/dev-doc/SPEC-FILE.md && echo OK` 输出 OK"
      - "SPEC-FILE.md 结构与 agents/spec-agent.md 中的 SPEC.md 输出结构一致（含 Goal/Scope/Design/Acceptance/Test Plan）"
      - "`test -f references/dev-doc/STATUS.md && echo OK` 输出 OK"
      - "STATUS.md 列出 STATUS.yaml 的必需字段（name/phase/mode/updated/started）及合法枚举值"

- [x] TASK-T003: agents/*.md 格式定义改为引用 references/
  - priority: P0
  - refs: SPEC-AC-002
  - files:
      create: []
      modify: ["agents/task-agent.md", "agents/spec-agent.md", "agents/prd-agent.md", "agents/test-agent.md"]
      test: ["tests/test_skills_docs.sh"]
  - depends_on: ["TASK-T001", "TASK-T002"]
  - parallel: false
  - complexity: M
  - done_when:
      - "agents/task-agent.md 中不再包含完整 task 文件模板（改为 `参见 references/dev-doc/TASK-FILE.md`）"
      - "agents/spec-agent.md 中不再包含完整 SPEC.md 模板（改为引用 references/dev-doc/SPEC-FILE.md）"
      - "`grep -c 'references/' agents/task-agent.md` 输出 >= 1"
      - "`grep -c 'references/' agents/spec-agent.md` 输出 >= 1"

- [x] TASK-T004: commands/*.md 格式定义改为引用 references/
  - priority: P0
  - refs: SPEC-AC-002
  - files:
      create: []
      modify: ["commands/task.md", "commands/issue.md", "commands/test.md"]
      test: ["tests/test_skills_docs.sh"]
  - depends_on: ["TASK-T001", "TASK-T002"]
  - parallel: true
  - complexity: M
  - done_when:
      - "commands/task.md 的输出格式段落标注 `读取 references/dev-doc/TASK-FILE.md`，不再内嵌完整字段模板"
      - "commands/issue.md 的写入格式段落标注 `读取 references/dev-doc/ISSUE.md`，不再内嵌完整字段模板"
      - "`grep -c 'references/' commands/task.md` 输出 >= 1"
      - "`grep -c 'references/' commands/issue.md` 输出 >= 1"

- [x] TASK-T005: 三处 SKILL.md 同步到最新状态
  - priority: P1
  - refs: SPEC-AC-003
  - files:
      create: []
      modify: ["skills/dev-flow/SKILL.md", ".claude/skills/dev-flow/SKILL.md", ".agents/skills/dev-flow/SKILL.md"]
      test: ["tests/test_skills_docs.sh"]
  - depends_on: []
  - parallel: true
  - complexity: M
  - done_when:
      - "三处 SKILL.md 均包含 `audit` 模式说明段落"
      - "三处 SKILL.md 均包含 VERSION 相关说明"
      - "`grep -c 'audit' skills/dev-flow/SKILL.md` 输出 >= 1"
      - "`grep -c 'VERSION' skills/dev-flow/SKILL.md` 输出 >= 1"
      - "三处 SKILL.md 的命令映射表一致（均含 /init /brainstorm /prd /spec /task /issue /devtest /fix /test /status /check /iterate /mode）"
      - "运行约定段落差异仅在 Codex/Claude Code/AGENTS 区分"

- [x] TASK-T006: validate.sh 对齐新字段
  - priority: P0
  - refs: SPEC-AC-004
  - files:
      create: []
      modify: ["scripts/init/validate.sh"]
      test: ["tests/test_validate.sh"]
  - depends_on: ["TASK-T001"]
  - parallel: false
  - complexity: M
  - done_when:
      - "validate.sh task 校验段支持 `done_when` 和 `priority` 字段（不再仅检查旧的 `Done when`/`level`）"
      - "validate.sh 新增对 `refs` 和 `files` 字段存在性的检查"
      - "`bash tests/test_validate.sh` 全部 PASS"

- [x] TASK-T007: references/dev-flow-spec.md 更新到当前实际状态
  - priority: P1
  - refs: SPEC-AC-005
  - files:
      create: []
      modify: ["references/dev-flow-spec.md"]
      test: ["tests/test_skills_docs.sh"]
  - depends_on: []
  - parallel: true
  - complexity: S
  - done_when:
      - "dev-flow-spec.md 反映当前流程（含 audit 模式、VERSION 机制、BRAINSTORM 不归档）"
      - "dev-flow-spec.md 中 task 文件格式描述与 references/dev-doc/TASK-FILE.md 一致（使用新字段名）"
      - "`grep -c 'audit' references/dev-flow-spec.md` 输出 >= 1"
      - "`grep -c 'VERSION' references/dev-flow-spec.md` 输出 >= 1"

- [x] TASK-T008: iterate.sh 归档 PRD/SPEC/TEST 改为 mv
  - priority: P1
  - refs: SPEC-AC-006
  - files:
      create: []
      modify: ["scripts/commands/iterate.sh"]
      test: ["tests/test_iterate.sh"]
  - depends_on: []
  - parallel: true
  - complexity: S
  - done_when:
      - "iterate.sh 第 169-171 行附近 PRD.md/SPEC.md/TEST.md 归档使用 `mv` 而非 `cp`"
      - "`grep 'cp.*ARCHIVE_DIR' scripts/commands/iterate.sh` 无输出（不含 cp 到归档目录的操作）"
      - "`bash tests/test_iterate.sh` 全部 PASS"
      - "执行 iterate 后 dev-doc/ 下不残留 PRD.md/SPEC.md/TEST.md"
