---
title: TASK - dow 跨平台分发与 CI
nums: 5
---

- [x] TASK-T001: dow version 子命令正式化
  - priority: P0
  - refs: user-request
  - files:
      create: []
      modify: ["dow/src/commands/version.rs", "dow/src/cli.rs", "dow/src/commands/mod.rs", "dow/src/main.rs"]
      test: ["tests/test_dow_version.sh"]
  - depends_on: []
  - parallel: true
  - complexity: S
  - done_when:
      - "`dow version` 输出 JSON `{\"version\":\"3.0.0\"}`"
      - "`dow version --set 4.0.0` 修改 VERSION 文件并输出 previous/action"
      - "`dow version --bump major` 正确递增"
      - "`dow version --set abc` 返回 exit 1 报错格式非法"

- [x] TASK-T002: 跨平台 wrapper + build.sh --dist
  - priority: P0
  - refs: user-request
  - files:
      create: []
      modify: ["scripts/bin/dow-wrapper", "dow/build.sh"]
      test: ["tests/test_dow_build.sh"]
  - depends_on: []
  - parallel: true
  - complexity: S
  - done_when:
      - "`bash dow/build.sh` 生成原生 `scripts/bin/dow`（ELF 二进制）"
      - "`bash dow/build.sh --dist` 生成 `dow-linux-x86_64` + wrapper 作为 `dow`"
      - "wrapper 模式下 `./scripts/bin/dow version -H` 正常输出"
      - "`scripts/bin/dow-wrapper` 对不支持平台返回 exit 1"

- [x] TASK-T003: .gitignore 配置
  - priority: P0
  - refs: user-request
  - files:
      create: []
      modify: [".gitignore"]
      test: []
  - depends_on: []
  - parallel: true
  - complexity: S
  - done_when:
      - "`.gitignore` 包含 `dow/target/` 排除编译缓存"
      - "`.gitignore` 不排除 `scripts/bin/dow*`（分发二进制需要提交）"

- [x] TASK-T004: GitHub Actions 多平台 CI
  - priority: P1
  - refs: user-request
  - files:
      create: [".github/workflows/build-dow.yml"]
      modify: []
      test: []
  - depends_on: ["TASK-T002"]
  - parallel: false
  - complexity: M
  - done_when:
      - "`.github/workflows/build-dow.yml` 存在且 YAML 合法"
      - "定义 matrix: linux-x86_64 / darwin-arm64 / darwin-x86_64 三个 target"
      - "每个 target 执行 `bash dow/build.sh --dist` 并 upload artifact"
      - "触发条件：push to main 且 `dow/` 目录有变更"

- [x] TASK-T005: 文档与 CLAUDE.md 更新
  - priority: P1
  - refs: user-request
  - files:
      create: []
      modify: ["CLAUDE.md", "AGENTS.md"]
      test: []
  - depends_on: ["TASK-T001", "TASK-T002"]
  - parallel: true
  - complexity: S
  - done_when:
      - "CLAUDE.md dow CLI 表中包含 `dow version` 行"
      - "CLAUDE.md 包含跨平台构建说明（`build.sh --dist`）"
      - "AGENTS.md 同步"
