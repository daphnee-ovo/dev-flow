---
title: TASK - 版本号单一真相源 + /iterate 重构
nums: 8
---

- [x] T1：创建 VERSION 文件 + 版本操作函数库
  - level: P0
  - details：在项目根目录创建 `VERSION` 文件（内容为 `2.2.0`）；新建 `scripts/lib/version.sh`，实现 `version_read()`、`version_validate()`、`version_bump()`、`version_write()`、`version_tag_exists()`、`version_create_tag()` 全部函数；编写对应的测试脚本验证每个函数的正确性
  - depends on：无
  - Done when：`cat VERSION` 输出 `2.2.0`；运行 `source scripts/lib/version.sh && version_read` 返回 `2.2.0`；`version_validate "1.2.3"` 返回 0，`version_validate "abc"` 返回非 0；`version_bump "2.2.0" minor` 输出 `2.3.0`；`version_bump "2.2.0" major` 输出 `3.0.0`；`version_bump "2.2.0" patch` 输出 `2.2.1`

- [x] T2：重构 /iterate 脚本（交付检查 + 归档 + commit & tag + bump 一体化）
  - level: P0
  - details：重写 `scripts/commands/iterate.sh`，实现完整 6 阶段流程：(1) 交付检查（task 全完成 + 无 P0 issue）；(2) 读取并校验 VERSION；(3) 归档到 `dev-doc/archive/v<version>-<topic>/`；(4) 展示变更摘要等待确认；(5) commit & tag；(6) bump VERSION + 重置 STATUS.yaml phase + 新 commit。source `scripts/lib/version.sh` 复用函数。支持 `$1` 为 topic、`$2` 为 bump 类型（默认 minor）
  - depends on：T1
  - Done when：在测试仓库中模拟完整迭代流程（创建 VERSION=2.2.0、模拟 task 全完成状态），执行 `bash scripts/commands/iterate.sh "test-topic" minor`，验证：(1) `git log --oneline -1` 包含 `Start v2.3.0`；(2) `git tag -l v2.2.0` 非空；(3) `cat VERSION` 输出 `2.3.0`；(4) `ls dev-doc/archive/v2.2.0-test-topic/` 目录存在

- [x] T3：修改 inject-context.sh 注入版本号
  - level: P1
  - details：修改 `scripts/hooks/inject-context.sh`，在输出行中加入版本号和 tag 同步状态。当 VERSION 存在时输出格式为 `[dev-flow <MODE>] v<VER>(<synced|no-tag>) | STAGE: ... | TASK: ... | ISSUE: ...`；不存在时保持原有格式不变
  - depends on：T1
  - Done when：(1) 创建 VERSION 文件后执行 inject-context.sh，输出包含 `v2.2.0(no-tag)` 字样；(2) 执行 `git tag -a v2.2.0 -m test` 后再次运行，输出包含 `v2.2.0(synced)`；(3) 删除 VERSION 文件后运行不报错，输出不含版本号

- [x] T4：修改 /status 命令展示版本信息
  - level: P1
  - details：修改 `scripts/commands/status.sh`，增加版本展示段落：读取 VERSION、检查对应 git tag 是否存在、输出 `当前版本：v<VER>（git tag: 已同步/未同步）`。VERSION 不存在时输出 `当前版本：未设置（缺少 VERSION 文件）`
  - depends on：T1
  - Done when：(1) 存在 VERSION=2.2.0 且无 tag 时，`bash scripts/commands/status.sh` 输出包含 `当前版本：v2.2.0（git tag: 未同步）`；(2) 打 tag 后输出包含 `已同步`；(3) 无 VERSION 文件时输出包含 `未设置`

- [x] T5：移除 STATUS.yaml 中的 iteration 字段
  - level: P1
  - details：从 `dev-doc/STATUS.yaml` 中删除 `iteration` 字段；检查所有脚本中读写 `iteration` 字段的代码并移除或替换为读取 VERSION 文件。涉及文件：`scripts/hooks/inject-context.sh`、`scripts/commands/iterate.sh`、`scripts/hooks/update-status.sh` 等
  - depends on：T1, T2, T3
  - Done when：`grep -r "iteration" scripts/` 无匹配（排除注释/changelog）；`grep "iteration" dev-doc/STATUS.yaml` 无匹配；所有脚本正常执行不报 iteration 相关错误

- [x] T6：重写 /iterate 命令文档（iterate.md）
  - level: P1
  - details：重写 `commands/iterate.md`，描述完整的迭代流程（含原 /done 职责）：交付检查 → 总结 → 归档 → commit & tag → bump。说明参数（topic、bump 类型）、前置条件、输出结果、错误处理
  - depends on：T2
  - Done when：`commands/iterate.md` 包含以下关键词/段落：交付检查、归档、commit & tag、bump、topic 参数、bump 类型（major/minor/patch）；文件大小 > 1KB

- [x] T7：废弃 /done 命令
  - level: P1
  - details：删除或清空 `commands/done.md`（保留文件并标注已废弃，重定向到 `/iterate`）；检查 skill 文件（`skills/dev-flow/SKILL.md` 或相关）中对 /done 的引用，更新为指向 /iterate；检查 CLAUDE.md 中的命令表，移除或标注 /done 废弃
  - depends on：T2, T6
  - Done when：`commands/done.md` 开头包含"废弃"或"deprecated"字样并指向 /iterate；`grep -l "/done" commands/ skills/ CLAUDE.md 2>/dev/null` 中所有匹配文件均已标注废弃或改为 /iterate

- [x] T8：P0 issue 修复自动 bump minor
  - level: P2
  - details：在 `/fix` 命令流程中，当关闭的 issue 为 P0 级别时，自动调用 `version_bump` 进行 minor bump 并写入 VERSION 文件，创建一条额外的 commit 记录版本变更。修改 `scripts/commands/fix.sh`（若存在）或在 fix 命令文档中说明此行为
  - depends on：T1
  - Done when：模拟关闭一个 P0 issue 后，VERSION 文件的版本号 minor 段 +1；关闭非 P0 issue 后 VERSION 不变
