#!/usr/bin/env sh
set -eu

dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
case "$(uname -s)-$(uname -m)" in
  Linux-x86_64|Linux-amd64)
    exec "$dir/dow-linux-x86_64" "$@"
    ;;
  Darwin-arm64|Darwin-aarch64)
    exec "$dir/dow-darwin-arm64" "$@"
    ;;
  *)
    echo "Unsupported platform: $(uname -s)-$(uname -m)" >&2
    exit 1
    ;;
esac
