---
title: TASK - batch 2026-08-02
nums: 1
---

- [x] TASK-T008: Improve CLI validation diagnostics for task and issue inputs
  - type: fix
  - priority: P1
  - refs: user-request; BRAINSTORM.md
  - files:
      create: ["dow/src/commands/input_validation.rs"]
      modify: ["dow/src/commands/mod.rs", "dow/src/commands/task.rs", "dow/src/commands/issue.rs", "plugin/commands/task.md", "plugin/commands/issue.md"]
      test: ["dow/tests/test_dow_task.rs", "dow/tests/test_dow_issue.rs"]
  - depends_on: []
  - parallel: false
  - complexity: M
  - done_when:
      - task create and issue create report all missing CLI fields in one failure and include expected formats or valid enum values
      - stdin JSON objects and arrays report all missing, type, unknown-field, enum, and nested files errors with record indexes, and malformed JSON includes its location
      - task create enforces the task schema enums for type, priority, and complexity while issue create/update preserve their declared validation rules
      - task and issue update plus --file JSON produce clear aggregated diagnostics and reject invalid input before any file is written
      - issue fix errors explicitly describe the resolve -> issue update --fix -> issue close lifecycle
      - regression tests cover the new diagnostics, batch atomicity, schema alignment, and valid existing inputs
      - task and issue command documentation matches the accepted JSON schema and fix lifecycle
